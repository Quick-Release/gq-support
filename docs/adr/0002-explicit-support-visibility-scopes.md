# Support visibility uses explicit scopes

A reporter's support visibility is governed by explicit scopes rather than WordPress role names: the default scope is the reporter's own requests, while an explicitly granted client support manager scope may cover all requests for one installation or an approved set of installations. This makes cross-installation access deliberate and prevents authorization from being inferred from GitHub membership or broad role labels.
