# Client support conversations use dedicated private repositories

Each client's support requests live in an approved dedicated private GitHub support repository rather than `gq-support` or a project repository. This keeps the GitHub-canonical, client-visible conversation separate from internal engineering discussion and chooses per-client isolation over the access risk of a shared support repository or a mixed project repository.
