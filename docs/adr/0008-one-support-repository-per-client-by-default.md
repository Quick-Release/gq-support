# One support repository per client by default

All installations of a client normally route to that client's dedicated private support repository. A separate per-installation repository is allowed only through an explicit isolation decision and approved mapping; reporters cannot select or change repository routing. This keeps routing predictable while preserving an escape hatch for genuine isolation requirements.
